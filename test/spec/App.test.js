'use strict';

define(function(require) {
    var App = require('App');

    describe('App', function() {
        it('should exist', function() {
            expect(App).to.exist;
        });
    });
});
